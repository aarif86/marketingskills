# Wildcard mode: one certificate for nsd.sg + *.nsd.sg via Cloudflare DNS-01.
{$BASE_DOMAIN:nsd.sg}, *.{$BASE_DOMAIN:nsd.sg} {
	tls {
		dns cloudflare {$CLOUDFLARE_API_TOKEN}
		resolvers 1.1.1.1 8.8.8.8
	}
	import common
}
